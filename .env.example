# Copy to .env for local runs. Never commit .env — it holds a real connection string.
PORT=8080

# Set from AOC-004 onwards; unused by the skeleton.
# DATABASE_URL=postgres://user:pass@localhost:5432/aoc_dev?sslmode=disable
